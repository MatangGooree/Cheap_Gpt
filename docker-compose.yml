version: '3.8' # Docker Compose 버전

services:
  web: # 이 서비스의 이름은 'web'으로 지정
    build: # build context 지정
      context: . # 현재 디렉토리를 빌드 컨텍스트로 사용
    ports:
      - '58080:58080' # 호스트와 컨테이너 포트 매핑
    environment:
      - NODE_ENV=production # 환경 변수 설정
